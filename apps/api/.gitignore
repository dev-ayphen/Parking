node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Test scripts (may contain phone numbers / secrets)
test_*.js

# Legacy upload directory (all media now in Supabase)
uploads/
