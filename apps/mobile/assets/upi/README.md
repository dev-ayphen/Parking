# UPI app logos

Drop the official app logos here with these EXACT filenames (PNG, square,
ideally 96×96 or larger, transparent background):

- `gpay.png`     — Google Pay
- `paytm.png`    — Paytm
- `bhim.png`     — BHIM

(PhonePe was intentionally removed from the UPI app row.)

Where to get the official logos (use each brand's press / brand-asset page):
- Google Pay: Google brand resources
- PhonePe: PhonePe press kit
- Paytm: Paytm brand assets
- BHIM: NPCI / BHIM brand assets

Each company owns its trademark — you (the app publisher) are responsible for
using their logos per their brand guidelines.

If a file is missing, the app falls back to a colored letter badge automatically
(see UPI_APPS in app/(find-space)/active-session.tsx), so nothing breaks.
